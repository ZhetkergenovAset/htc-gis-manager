package com.dilau.server.repository;

import com.dilau.server.domain.HistoryBuildings;
import org.springframework.data.jpa.repository.JpaRepository;

public interface HistoryBuildingsRepository extends JpaRepository<HistoryBuildings,Long> {
}
